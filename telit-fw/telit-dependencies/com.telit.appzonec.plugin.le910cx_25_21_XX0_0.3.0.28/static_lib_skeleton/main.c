/*
 * AppZone C Static Library main file.
 *
 * The user can define methods that will be used by applications using this library.
 * static_lib_example_method() is just an example and would be better to delete.
 *
 * It is recommended that the user provides header files alongside the library itself.
 *
 * To use the library in an AppZoneC project. Just drag and drop the produced file (.azsl)
 * to the AppZoneC project structure. Make sure the library and application are compatible
 * both in terms of product (Telit module) and firmware.
 *
 */

// PLEASE REPLACE WITH USER-DEFINED METHOD
int static_lib_example_method()
{
	return 0;
}
